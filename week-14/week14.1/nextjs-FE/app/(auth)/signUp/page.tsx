/*
The export default keyword allows this component (Signup) to be the default export from this file.
This means that when another file imports this module, it can import it without using curly braces {} and can rename it if needed.
*/
export default function Signup(){
    return (
        <div>
            Sign up
        </div>
    )
}